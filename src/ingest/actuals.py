"""gridstatus pulls: published LMPs and dispatch.

HELD OUT. This module is an answer key, not a model input. Its output may reach
src/validate/compare.py and nothing else. It must never appear in a Scenario."""
