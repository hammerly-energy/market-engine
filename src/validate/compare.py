"""Compare model output against held-out actuals from src/ingest/actuals.py.

Structure is what should match: when prices separate, when the ramp bites, which
hours go negative. Absolute levels will be wrong."""
